ScriptName Cars
# Script to contain information of cartrige and sample
# to be called by both LMMCars and MMMCars. Then,
# cat[1], name[1], name1... are all available for string use.
# cartrige number is set to registration number for LMMCars and MMMCars.

## define where to save 
SetDirectory X:\Workshop_20190501
#UserSetDirectory 

## define cartirges and sample names, do not change "cat" and "name"
cat = {  2 3  } 
name = { RA7-g1 RA7-g2 }


#### NO EDITING BELOW ####
Loop $#cat ind
   cat$ind = $cat[$ind]
   echo cat$ind = $cat[$ind]
EndLoop 

Loop $#name ind
   name$ind = $name[$ind]
   echo name$ind = $name[$ind]
EndLoop
